[project]
name = "byte"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.4.1",
    "python-dateutil>=2.9.0.post0",
]

[dependency-groups]
dev = [
    "ty>=0.0.1a11",
]
